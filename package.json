{
  "name": "seniors-td",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:fast": "vitest run --exclude '**/node_modules/**'",
    "typecheck": "tsc --noEmit",
    "sim": "node src/headless.ts",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
