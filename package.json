{
  "name": "seniors-td",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:fast": "vitest run --exclude '**/node_modules/**' --exclude '**/balance.test.ts'",
    "typecheck": "tsc --noEmit",
    "sim": "node src/headless.ts",
    "campaign": "node src/campaign.ts",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@vitest/coverage-v8": "^5.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^5.0.0"
  }
}
